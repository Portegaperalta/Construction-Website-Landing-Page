import { Button } from "./button"

export default function ContactButton() {
  return (
    <div className="contact-button">
      <Button className="text-white bg-(--clr-primary)">
        Contact Us
      </Button>
    </div>
  )
}